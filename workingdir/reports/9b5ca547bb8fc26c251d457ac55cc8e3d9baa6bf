ERROR:ASSIGN_IN_IF: do not use assignment in if condition
#71: FILE: drivers/gpu/drm/nouveau/nvkm/engine/disp/sorgm200.c:154:
+	if (!((hda = nvkm_rd32(device, 0x08a15c)) & 0x40000000))

ERROR:ASSIGN_IN_IF: do not use assignment in if condition
#127: FILE: drivers/gpu/drm/nouveau/nvkm/engine/disp/sorgp100.c:87:
+	if (!((hda = nvkm_rd32(device, 0x08a15c)) & 0x40000000))

ERROR:ASSIGN_IN_IF: do not use assignment in if condition
#182: FILE: drivers/gpu/drm/nouveau/nvkm/engine/disp/sorgv100.c:141:
+	if (!((hda = nvkm_rd32(device, 0x08a15c)) & 0x40000000))

WARNING:LINE_SPACING: Missing a blank line after declarations
#238: FILE: drivers/gpu/drm/nouveau/nvkm/engine/disp/sortu102.c:126:
+	u32 hda = nvkm_rd32(device, 0x08a15c);
+	if (hda & BIT(id))

total: 3 errors, 1 warnings, 196 lines checked

NOTE: For some of the reported defects, checkpatch may be able to
      mechanically convert to the typical style using --fix or --fix-inplace.

Commit 9b5ca547bb8f ("drm/nouveau/disp/gm200-: detect and potentially disable HDA support on some SORs") has style problems, please review.

NOTE: If any of the errors are false positives, please report
      them to the maintainer, see CHECKPATCH in MAINTAINERS.
